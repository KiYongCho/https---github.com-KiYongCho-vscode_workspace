// 자바스크립트 엔진은 자바스크립트 코드를 위에서 아래로 한 줄씩 순차적으로 실행

let i = 0; // i변수에 0 할당(=저장=대입)
console.log(i); // 0, 화면에 i변수의 값을 출력
i = i + 1; // i변수의 값에 1을 더해서 다시 i변수에 할당
i = i + 1;
i = i + 1;
i = i + 1;

// 자동완성 코드조각 설정
// File > Preferences > Configure Snippets > javascript 검색
// prefix, body 설정
console.log('');




